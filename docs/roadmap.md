# Zona 实现路线图

## 第一阶段：解释器 / REPL ✅

1. 词法器 ✅
2. 数据栈 + 字面量压栈 ✅
3. 算术和比较运算符 ✅
4. `.` 打印 ✅
5. 栈操作原语（`:dup` `:drop` `:swap` `:over` `:rot`）✅
6. 字定义（`@` `;`）和调用 ✅
7. 控制流（`?` `!` `~` `$`）✅
8. 内存（`&` `#` `:here` `:allot`）✅
9. 字符串 ✅
10. REPL 交互模式 ✅
11. 文件执行模式 ✅
12. `:type` `:emit` `:key` IO 原语 ✅
13. `:stack` 调试 ✅
14. `:use` 文件引入 ✅
15. 标准库 `std/` ✅
16. `:alloc` `:free` 动态内存 ✅
17. `:time` `:rand` `:exit` 系统原语 ✅
18. `:argc` `:argv` 命令行参数 ✅
19. `:fopen` `:fread` `:fwrite` `:fclose` 文件读写 ✅
20. VS Code 语法高亮 ✅
21. 错误信息加行号 ✅
22. 测试框架 `std/test.zona` ✅
23. REPL 历史记录（readline）✅

## 第三阶段：预备方案（已设计，待实现）

详见 `spec.md` 对应章节。

1. ⭐⭐⭐ 命名模块系统 — 见 `spec.md` 第八章（模块表 + `:use` 扩展 + 词法 `字.字` 合并，预计 ~200 行 C）
2. ⭐⭐⭐ `.` 成员访问 — 见 `spec.md` 2.1 和第九章（词法层 `字.字` / `字.'str'`）
3. ⭐⭐⭐ 结构体支持 — 见 `spec.md` 第九章和 `ffi.md`（`:struct` + 构造器方案 B）
4. ⭐⭐ 字符串模板 — `'hello $name'`
5. ⭐⭐ 错误处理机制
6. ⭐⭐⭐ 数字类型系统 — `3u8` 后缀精度
7. ⭐⭐⭐ 栈类型系统 — 带类型标记

## 已讨论不需要的

- 尾递归优化、块注释、列号报错、HTTP/异步
- 模块分隔符用 `"`、`,` 替代 `.` 做 print、`:reexport` 原语

## 第二阶段：QBE 编译后端 ✅

`zonac.c`（1192 行），复用 `zona.h` 前端，生成 QBE IR，编译为原生可执行文件。

已实现：
1. 数字、算术、比较运算 ✅
2. `.` 打印（整数/浮点自动判断）✅
3. 栈操作原语（`:dup :drop :swap :over :rot`）✅
4. 字定义和调用 ✅
5. 控制流（`? ! ~ $`）✅
6. 内存（`& # :here :allot :alloc :free`）✅
7. 字符串字面量 + `:type :emit :key` ✅
8. 文件 IO（`:fopen :fread :fwrite :fclose`）✅
9. 命令行参数（`:argc :argv`）✅
10. `:use` 文件引入 ✅
11. `:stack` 调试 ✅
12. `:time :rand :exit` 系统原语 ✅
13. `:bind` FFI 绑定 ✅
14. `:use` / `:bind` 编译期校验 ✅

所有原语与解释器完全对齐（REPL 模式除外）。

## 设计备忘

- 宿主语言：C
- 解释器：`cc src/zona.c -o zona -lm -lreadline`
- 编译器：`cc src/zonac.c -o zonac -lm`（依赖 QBE）
- `:use` 路径显式前缀（`./` `../`）为未来标准库保留空间
- `[ ]` `( )` `{ }` 是最有价值的待选符号，预留给错误处理等关键功能
- 字可覆盖重定义，后定义遮盖先定义
