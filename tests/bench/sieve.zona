_ 数组填充+求和 — 用 :stack 验证过的栈模式

_ 分配 n 个 cell，返回基址
@ allocArr :here :swap :allot ;

_ 填充: arr i n → (直到 i=n，填 arr[i]=i)
@ fillStep :rot :dup :rot :rot # :rot 1 + :rot :rot ;
@ fillLoop :over :over = ? $ fillStep ~ ;

_ 求和: arr i n sum → (直到 i=n，累加 arr[i])
@ sumStep :rot :dup :rot :rot + & :rot + :swap 1 + :swap ;
@ sumLoop :over :over = ? $ sumStep ~ ;

_ 主程序
1000 allocArr     _ 分配数组，栈上只剩基址
:dup 0 :over fillLoop   _ 填 arr[i]=i
:swap 0 1000 0 sumLoop . _ 求和: 0+1+...+999 = 499500